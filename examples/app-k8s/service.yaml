apiVersion: v1
kind: Service
metadata:
  name: minha-app-svc
  namespace: apps
  labels:
    app: minha-app
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  type: ClusterIP
  selector:
    app: minha-app
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP